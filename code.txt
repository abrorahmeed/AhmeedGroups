<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Summage</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Poppins", "Segoe UI", Arial, sans-serif;
    }

    body {
      background: linear-gradient(135deg, #f9fcff, #eef6ff);
      color: #1a2a40;
      line-height: 1.6;
    }

    /* Header */
    header {
      position: sticky;
      top: 0;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 2rem;
      background: rgba(31, 60, 136, 0.9);
      color: white;
      backdrop-filter: blur(8px);
      z-index: 1000;
    }

    .header-left {
      display: flex;
      gap: 1.5rem;
      font-size: 0.9rem;
      opacity: 0.9;
    }

    .header-left span:hover {
      text-decoration: underline;
      cursor: pointer;
    }

    .header-center {
      font-size: 1rem;
      font-weight: 500;
      letter-spacing: 0.5px;
    }

    .header-right h1 {
      font-size: 1.6rem;
      font-weight: 700;
      letter-spacing: 1px;
    }

    /* Main Layout */
    main {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 2rem;
      padding: 4rem 8%;
      min-height: 90vh;
    }

    .left-side {
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      gap: 1.5rem;
    }

    .left-side h1 {
      font-size: clamp(2rem, 4vw, 3.5rem);
      color: #1f3c88;
      font-weight: 700;
      letter-spacing: -1px;
    }

    .left-side img {
      width: 100%;
      max-width: 500px;
      border-radius: 18px;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.12);
      transition: transform 0.4s ease;
    }

    .left-side img:hover {
      transform: scale(1.02);
    }

    .right-side {
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 1.2rem;
      align-items: center;
    }

    .right-side button {
      padding: clamp(0.8rem, 1.2vw, 1.2rem) clamp(1.5rem, 2vw, 2.2rem);
      font-size: clamp(1rem, 1.2vw, 1.3rem);
      font-weight: 500;
      border: none;
      border-radius: 50px;
      background: linear-gradient(135deg, #3f72af, #345fa6);
      color: white;
      cursor: pointer;
      width: clamp(200px, 60%, 280px);
      box-shadow: 0 4px 12px rgba(63, 114, 175, 0.3);
      transition: all 0.3s ease;
    }

    .right-side button:hover {
      background: linear-gradient(135deg, #345fa6, #2c4f8c);
      transform: translateY(-2px);
      box-shadow: 0 6px 16px rgba(63, 114, 175, 0.4);
    }

    /* Tablet */
    @media (max-width: 992px) {
      header .header-left, header .header-right {
        display: none;
      }
      header .header-center {
        margin: 0 auto;
        font-size: 1.1rem;
      }
      main {
        flex-direction: column;
        text-align: center;
        padding: 3rem 5%;
      }
      .left-side, .right-side {
        align-items: center;
      }
      .left-side h1 {
        font-size: 2.2rem;
      }
      .left-side img {
        max-width: 400px;
      }
    }

    /* Mobile */
    @media (max-width: 600px) {
      header {
        display: none;
      }
      main {
        padding: 2rem 1.5rem;
      }
      .left-side h1 {
        font-size: 1.8rem;
      }
      .left-side img {
        max-width: 100%;
      }
      .right-side button {
        width: 100%;
      }
    }
  </style>
</head>
<body>

  <!-- Header -->
  <header>
    <div class="header-left">
      <span>About Us</span>
      <span>FAQ</span>
      <span>Contact</span>
    </div>
    <div class="header-center">Education without barriers</div>
    <div class="header-right"><h1>Summage</h1></div>
  </header>

  <!-- Main -->
  <main>
    <div class="left-side">
      <h1>Summage</h1>
      <img src="https://source.unsplash.com/800x500/?education,books,learning" alt="Education Image">
    </div>
    <div class="right-side">
      <button>Begin Studying</button>
      <button>About Summage</button>
      <button>Summage Blogs</button>
    </div>
  </main>

</body>
</html>
